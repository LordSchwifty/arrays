var friends = ["jack", "will", "max", "clarke"];

var sneakerPrices = [110, 150, 225, 325];

var studiesTooMuch = {true, false, false, true};

students.push("rory")
// will add another friend "rory " to the end of the array
sneakerPrices.pop(1)
// will remove the last element of the array
studiesTooMuch.unshift(false)
// will add another element to the beginning of the array and change the array